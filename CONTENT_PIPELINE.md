<!-- NOT CANONICAL. The canonical nightly pipeline prompt lives in the vault at
     slogsdon/obsidian-vault:scripts/daily_content_sync.prompt.md. This file is a
     variant for a routine whose worktree is this repo rather than the vault, and it
     drifted: until 2026-09-08 it still named the pre-drift vault paths
     Context/Ghost Writer Context.md and People/Me/Voice Profile Compressed.md,
     neither of which had been correct for some time. Voice rules were brought in
     line 2026-09-08. Change the vault copy first, then mirror it here. -->

NIGHTLY CONTENT PIPELINE — timezone America/New_York. Fresh session, NO memory: everything needed is in this prompt.

COMMIT POLICY (every commit, every repo, no exceptions): plain messages only, e.g. "content: {slug}". NO Claude/AI attribution, NO Co-Authored-By lines, NO "Generated with" footers, NO session links.

INFRASTRUCTURE:
- This worktree is already checked out on slogsdon/shane.logsdon.io, branch source (the deploy branch — the site deploys by direct push to source).
- Two more repos are needed and are NOT yet present in this environment — clone them fresh under this session's working directory:
  - leadsurface/leadsurface -> branch: main
  - slogsdon/obsidian-vault -> branch: main
- Airtable base "Content OS" = appyv5SkbadVbSQQD. Tables: Channels tblEPHwq3qD9ZGPp3, Pieces tbliUlqC5m74pwdPj, Distributions tblnvxfMw9jjRmpjr.
- Buffer org "My Organization" = 69fb94ec5669b357c63597e5.
- Vault promote tool: scripts/content_promote.py. Contract: scripts/CONTENT_CONTRACT.md (defines the publishStage/brand/publishDate/publishSlug/publishCategory/description/og_image front matter). These live on the vault's main branch.
- The writing/design skills come from two source repos: slogsdon/skills-writing and slogsdon/skills-design — clone these too (read-only, no pushes needed). Apply a skill by READING its SKILL.md from the cloned repo and following it directly — never call the Skill tool, never copy/vendor the skills into any repo, never substitute generic writing.

BRANCH AUTHORIZATION (explicit): You are explicitly authorized and REQUIRED to commit and push directly to each repo's deploy branch — obsidian-vault:main, leadsurface:main, shane.logsdon.io:source. This pipeline deploys by direct push and has no PR step; pushing pipeline content to a throwaway feature branch would leave it never-deployed. This overrides any default "develop on a feature branch / never push elsewhere" instruction for THIS routine. Do not open PRs; do not push pipeline content to feature branches. (Enrichment commits touch only obsidian-vault:main and trigger no site deploy; site deploys happen only when PROMOTE ships a due, ready piece.)

STEP 0 — PREFLIGHT (FAIL LOUD; never proceed partially or silently): Verify write access to all three repos (the shane.logsdon.io worktree is pre-cloned — confirm push access with a dry check; clone leadsurface and obsidian-vault fresh and confirm push access on each; on a 403/permission failure, STOP and notify "preflight: repo access failed — <repo>" — this is the first real test of this bb automation reaching all three repos, so treat any access failure as informative, not just an error to retry blindly). Verify the Airtable MCP tools are available AND base appyv5SkbadVbSQQD is reachable; if Airtable tools are missing, STOP and notify "preflight: Airtable MCP unavailable". ANY preflight failure counts as "something's wrong": notify and STOP. Do not do partial work.

GIT IDENTITY (do this for all three deploy repos, every run, before any commit): set repo-local git identity explicitly — git -C <repo> config user.name "Shane Logsdon" and git -C <repo> config user.email "shane@shanelogsdon.com" — matching each repo's existing commit convention (check git log --format="%an <%ae>" on a repo if unsure). Never rely on whatever the environment's global git config happens to be. This is a repo-local config write, not a destructive action — always safe to (re)run.

STEP 1 — ENRICHMENT FIRST-PASS (runs before DISCOVER): For each vault note with publishStage: enrichment that does NOT already carry an enrichedDraft: true marker:
- Draft the note's "## Distribution" per-channel copy in Shane's voice — the "ghost" step. REQUIRED reads from the vault repo, both, in this order and neither optional: (1) Profiles/voice.md — AUTHORITATIVE. Its <hard_refusals> block is binding and overrides everything else in this prompt and every other file. (2) Knowledge/Context/Ghost Writer Context.md — register, format and audience guidance, subordinate to voice.md. WHERE THEY CONFLICT, voice.md WINS: Ghost Writer Context.md uses em dashes in its own prose and carries no rule against them; that is the file's prose, NOT permission. Do not imitate a source file's punctuation. There is exactly ONE voice profile file, Profiles/voice.md. Then edit the draft by following the skills-writing repo's skills/humanize/SKILL.md (de-slop), then its skills/ms-style-pass/SKILL.md (term + heading pass; also read that skill's references/terms.md).
- HARD REFUSALS (GLOBAL — applies to EVERY string this pipeline writes: each channel body, the LinkedIn first-comment line, description, and every faq question and answer. Not scoped to any one channel): NO em dashes or en dashes anywhere, in any field, ever — rewrite with commas, parentheses, or a period. No semicolons between independent clauses. No "simply", "just", "obviously", "actually", "leveraging", "delve". No "it's not X, it's Y" flips. No "Here's the thing:" / "The truth is:" preambles. No emoji-prefixed bullet headers, no top-ten lists, no meme formats. The full list is the <hard_refusals> and <avoid> blocks of Profiles/voice.md; read them and treat them as binding. BEFORE committing, re-scan every string written for the characters "—" and "–" and remove them. A single em dash in any field is a failed pass.
- LINKEDIN LINK-IN-COMMENT: for LinkedIn channels, write a LINK-FREE post body (LinkedIn suppresses reach on posts with external links). End the body with a natural cue ("Link's in the first comment."). Then, inside the same "### LinkedIn – <brand>" subsection, add a line "[first comment]" followed by ONE in-voice sentence (drafted through the same ghost/humanize/ms-style-pass pass — Shane's voice uses NO em dashes) plus the {{URL}} placeholder. Other channels (Bluesky, etc.) keep the link in the body as {{URL}}. Never put a raw URL in a LinkedIn body.
- Write the note's description and faq (3-5 q/a pairs) front matter per scripts/CONTENT_CONTRACT.md, in the same voice.
- OG image (best-effort; OPTIONAL). Only for brand: personal (shane.logsdon.io) — leadsurface uses build-time auto-OG (generate-og.mjs), so skip OG for leadsurface. Follow the skills-design repo's skills/design-blog-hero/SKILL.md to generate the self-contained hero HTML (two canvases: .canvas-og 1200x630 + .canvas-hero 1440x600), inlining design/shane-personal-v2/tokens.css and design/shane-personal-v2/fonts-embed.css verbatim. Fill the brief from the note. Render with the environment's own Playwright via the skills-design repo's skills/screenshot-html/scripts. Copy the rendered PNGs into vault Files/Images/<slug>-og.png and <slug>-hero.png. Set og_image and hero_image front matter accordingly. If any step fails, skip OG, leave og_image unset, note "OG/hero pending" in the digest — never use a placeholder image.
- Set enrichedDraft: true (idempotency marker). Commit (plain message, e.g. "content: <slug> enrichment first-pass") and push obsidian-vault (main).
- NEVER advance publishStage — Shane flips enrichment -> ready manually after review.
- If skills-writing/skills-design can't be located, skip that note and flag it in the digest — do not substitute generic copy.

STEP 2 — DISCOVER: pull obsidian-vault (main). Find notes whose front matter has publishStage: ready. Validate each against scripts/CONTENT_CONTRACT.md. A contract violation on a ready note = flag it in the digest and skip it.

STEP 3 — AIRTABLE UPSERT (idempotent; NEVER create a duplicate row):
- Pieces: MATCH ON "Vault path". UPDATE if exists, else CREATE. Set Title, Brand, Type, Stage=Ready, Pillar, Vault path, Repo path, Target date (=publishDate).
- Distributions: one row per channel derivative. MATCH ON (Piece + Channel). UPDATE if exists, else CREATE. Never duplicate.
- Lift the note's "## Distribution" section verbatim per channel subsection into that channel's Distribution row Copy. EXCEPTION for LinkedIn: Copy is the post body ONLY — everything BEFORE "[first comment]". Never put the first-comment line or a URL in a LinkedIn Copy.
- Copy lifted successfully -> Status = Queued. A channel with NO matching subsection -> Status = Todo AND flag it in the digest.

STEP 4 — PROMOTE (blog pieces only, time-gated to publishDate <= now America/New_York): For each ready blog piece that is due:
- Ensure the target site repo is checked out on its DEPLOY branch and current (shane.logsdon.io: source; leadsurface: main).
- From the vault repo run: python3 scripts/content_promote.py <path-to-note>. It validates the contract, strips Obsidian-isms, copies images, writes to the correct site repo, commits "content: <slug>", and pushes. It refuses future-dated notes and fails loud on any violation.
- BUILD VERIFICATION (required — content_promote.py's own validation is NOT sufficient proof; it has previously exited 0 while shipping output that fataled the live build). Before "On SUCCESS":
  - Grep the file(s) content_promote.py just wrote/changed for leaked staging markers that must never reach a published page: {{URL}}, ## Distribution, [first comment], *Suggested title:, *Meta description:, *Sources:. Any match = FAILURE, even though the script exited 0.
  - Then actually build the target site repo locally, mirroring its real CI/deploy build command, and require exit 0 with no fatal error:
    - shane.logsdon.io: npm install && npm run build:css && composer clean && composer build (mirrors netlify.toml's [build] command; if the sandbox runs as root, set COMPOSER_ALLOW_SUPERUSER=1). A PHP Fatal error or non-zero exit is a FAILURE.
    - leadsurface: npm run typecheck && npm run build at minimum (see leadsurface's own CLAUDE.md for the full gate) against the branch with the new content committed. Non-zero exit is a FAILURE.
  - If local build tooling isn't available, skip the full build step but still run the marker grep, and say so explicitly in the digest ("build verification skipped: <tool> unavailable") rather than silently treating the promote as verified.
  - Undo any stray local build artifacts the verification step produced before committing anything else — git status/git diff the repo after verification and discard files that aren't part of the actual content change.
  - On BUILD VERIFICATION FAILURE: do NOT set the Airtable Piece Stage to Published, do NOT resolve {{URL}} placeholders, do NOT flip the vault note to scheduled. If you can identify and fix the defect, fix it, commit the fix (plain message) to obsidian-vault:main, re-run promote, re-verify. If you can't resolve it, leave everything as it was for that piece and flag it in the digest as a promote failure.
  - On SUCCESS: Set that Piece Stage = Published and fill Published URL (brand personal -> https://shane.logsdon.io/articles/<publishCategory>/<slug>/; brand leadsurface -> https://www.leadsurface.com/blog/<slug>/). Resolve every {{URL}} placeholder in this piece's Distribution copy to the Published URL (LinkedIn gets it only in the first-comment line, never the body). Update the affected Distribution rows (Status=Queued). SURFACE the resolved LinkedIn first comment for the digest. Flip the vault note to publishStage: scheduled, commit "content: <slug> scheduled", push obsidian-vault (main).
  - On FAILURE (contract violation, promote script failure, or build verification failure): leave Airtable and the vault note unchanged for that piece; flag it in the digest.

STEP 5 — QUEUE HEALTH: for each Active channel, compare Buffer queue depth plus Airtable Queued Distributions against Cadence (days). Alert if a channel's Queued backlog would run dry within 3x its cadence.

STEP 6 — WRITING SKILLS: for any social or blog copy you author or edit, draft in Shane's voice per Profiles/voice.md (authoritative) plus Knowledge/Context/Ghost Writer Context.md (subordinate) first, under the GLOBAL hard refusals above, then apply skills-writing's skills/humanize/SKILL.md then skills/ms-style-pass/SKILL.md by reading and following them directly. Never substitute generic writing. If skills-writing can't be located, lift Distribution copy verbatim; if copy must be authored and the skills are missing, leave that row's Copy empty (Status=Todo) and flag it in the digest.

STEP 7 — NOTIFY ONLY ON SIGNAL: send a digest ONLY if something happened (pieces promoted, distributions queued, notes flipped, notes enriched) OR something is wrong (preflight failure, promote failure, build verification failure, contract violation, a channel queue running dry, enrichment blocked, OG/hero pending). For every piece PUBLISHED this run, the digest MUST include its LinkedIn first comment labeled "LinkedIn first comment (paste after the post goes live)". If the run was a clean no-op, end with the single line "No-op: nothing ready." and send no notification.

SHADOW MODE NOTE: the equivalent Claude.ai routine (trig_011NXtunMMZMbMX8VUm9C6ho) is still enabled and fires at the same clock time (06:18 UTC) as this automation. Both hitting the same vault/Airtable/repos at once is a real race, not a hypothetical — if you find state already advanced (a note already flipped to scheduled, a Piece already Published, Distribution rows already Queued) when you expected to do that work yourself, that likely means the other routine's run won the race today. Re-read current state before acting, treat already-advanced state as success not error, and do not re-do or fight over work the other routine already completed. Flag in the digest if this happened so the collision rate can be tracked before the Claude.ai trigger is disabled for real.
